PER PULSANTI:
-attivare GPIO mode: External Interrupt mode with Falling edge
-su NVIC attivare EXTI line[15:10] Interrupts
-Su project manager, attivare su code generator "Generate pheripheral initialization as ..."
-andare sul file system_stm32fyxx.c (contiene tutti i vari callback)
-premere F3 su HAL_GPIO_EXTI_IRQHandler
-scrivere nel file "gpio.c", sezione UserCodeBegin2,  
void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin){
	if(GPIO_Pin == BUTTON_Pin){
        if (HAL_GPIO_ReadPin(BUTTON_GPIO_Port, BUTTON_Pin) == GPIO_PIN_RESET) {
            // Se il pulsante è premuto (stato LOW)
            HAL_GPIO_WritePin(LED_GPIO_Port, LED_Pin, GPIO_PIN_SET); // Accendi LED
        } else {
            // Se il pulsante non è premuto
            HAL_GPIO_WritePin(LED_GPIO_Port, LED_Pin, GPIO_PIN_RESET); // Spegni LED
RISULTATO: Se pulsante premuto -> r2d = true,
quando viene rilasciato -> r2d = false;

PULSANTE cambiaPagina

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

CAN SETUP: (Se si usa MCP2515 -> vedere file CAN.c e CAN.h salvati in "File utili")
-Attivare RX0 interrupt
-file main.c -> configuare filtro e i pacchetti r2d e map da inviare; preparare anche le variabili che contengono i rispettivi bit/byte CAN 
CAN GESTIONE RICEZIONE:
-Gestione ricezione CAN su CAN_RX0_IRQ_Handler in "stm32fyxx_it.c -> sezione userCodeBegin CAN1_RX0_IRQn 1
-scrivere tutte le variabili che si usano nell'interrupt del (es)gpio.c sul relativo file.h con extern
-Organizzazione dell'ordine dei vari dati in Data[Ndata] --> ogni parametro avra una posizione nota e fissa
-gestione cambioPagina
-attivazione flag del rispettivo elemento quando lo si riceve
-attivazione attive del rispettivo elemento quando è attiva la pagina su cui esso è presente


----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

+INVIO in UART -> Nextion_SendString

TODO
parte manettini-shift register

Domande:
-Quando si avvia la macchina/volante, che mappa si deve impostare?
-can_filter.FilterFIFOAssignment = CAN_FILTER_FIFO0;	//CAN_RX_FIFO0	quale devo usare?
-Invio dei data CAN tramite flag su ciclo while? -> Altrimenti se su Callbacks crash perchè non è possibile delay
-r2d -> se premo una volta si attiva (se anche freni...), se premo nuovamente si spegne(..)?
-ID-CAN Stato di carica?

++ID PER ASMS On, ++ID per TS Active


DA PROVARE:
-CAN-BUS
-SHIFT-REGISTER
	